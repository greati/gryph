sub range(begin : int; end : int; step : int = 1) : [int] {
	l : [int] = [];
	count : int = begin;
	
	while(count < end){
		l = l ++ [count];
		count = count + 1;
	}
	return l;
}

print range(0, 10, 1);
