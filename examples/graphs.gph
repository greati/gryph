#a = <[1,2,3], a<-b for a,b over [1,2,3] when a==2, b==3>;
#g1 : <int> = <[1,2,3], a->b for a,b over [1,2,3],[1,2,3]>;
#g2 : <int, float> = <a/2 where a--b+1 for a,b over [0,1],[1,2,3]>;
#g3 : <int> = <a--a for a over [0, 1, 2] when a > c>;

a = <[1,2,3], a->b for a,b over [1,2,3],[1,2,3]>;

a = <a<-b for a,b over [1,2,3]>;
