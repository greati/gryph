#a=k*2+1;
#a,b = 1^2^3, 2;
#if ((2>3 and 2<4) == true)


#a : <[int]> = 4;

#a : (int, int) = (|"oi"?2|);

sub m (a:|int,string|=|1?"oi",2?"vitor"|) {
    a = 2;    
}

sub g (a,b:int) : int {
    return a + b;
}

sub f (a,b:int, k:string, m:[bool]) : [int] {
    a = 2;
}

a : int;
b : int;


a = n;

if (a > b) {
    print b;
} else {
    print b;
}
