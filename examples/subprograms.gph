sub g(a : int; b : int; c : int) {
    print a + b - c;
}

sub f(a : int; b : int; c : int) {
    print a + b;
}

sub h (a, b : int) : int {
    i : int = b;
    while (i < a) {
        if (i % 2 == 0) {
            return i;
        }
        i = i + 1;
    }
}

sub k(a,b:int=1,2) {
    print a+b;
}

print h(20,15);

