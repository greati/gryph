sub mdc (n, m : int) : int {
    if (m % n == 0) return n;
    else if (n % m == 0) return m;
    else if (m > n) return mdc(n, m % n); 
    else return mdc(n % m, m); 
}

x : [(int,int)];
x = [(a,b) for a,b over [2,3,4,5],[6,7,8,9,10]];
print x;

add (10,10) in x;
print x;

y : [[int]] = [[1,2,3],[3,4]];
add [] in y;
print y;

del 1 from y;
println y;

ll : [int] = [2*a for a over [1,2,3] when (a == 2)];
println ll;



