sub mdc (n, m : int) : int {
    if (m % n == 0) return n;
    else if (n % m == 0) return m;
    else if (m > n) return mdc(n, m % n); 
    else return mdc(n % m, m); 
}

x : [int];
x = [(a,b) for a,b over [2,3,4,5],[6,7,8,9,10] when false];
print x@string;

