#a, b : int = 2 + 2, 2^3;

#c : int = b + 1;
d : [[int]];

#d = [[1,2,3]];

#a : bool = true;

e : |string, int| = | "a" ? 0, "b" ? 1,  "c" ? 2 |;

e = |"b" ? 1|;

l : [int] = [];

l = [1,2,3];

l = [];

#print (d % a )[0];
