#d : [[int]];

#g : |string,  int| = | "a"? 0, "b"? 1,  "c"? 2 |;

i : int = 0;


while (i != 10) {
    x : int = i + 1;
    print i;
    print x;
    i = i + 1;
}

#e = |"b" ? 1|;

#l : [int] = [];

#l = [1,2,3];

#l = [];

# j : int = f.0;
#k : (int, int) = (0,1);

#j : int = k.1;

# print j;
