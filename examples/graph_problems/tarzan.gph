sub dist(a : (int,int); b : (int,int); reach : int) : bool{
	if (((a\0\ - b\0\) ^ 2 + (a\1\ - b\1\) ^ 2 ) <= reach ^ 2)
		return true;
	else
		return false; 
}

#nodes : [(int,int)] = [(1,1),(6,1),(6,6),(11,6)];
nodes : [(int,int)] = [(1,1),(6,6),(11,6),(13,8)];
trees : int = 4;
reach : int = 5;
count : int = 0;

g : <(int,int)> = <a--b for a,b over nodes when (dist(a,b,reach))>;

bfs a in g
	count = count + 1;

if(count == trees)
	println "S";
else
	println "N";
