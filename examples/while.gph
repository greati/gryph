i : int = 0;

a : int;

for a over [1,2,3] {
    print a;
    if (a == 2) {
        break;
    }
}


sub f (a:int) : int {

    i : int = 0;

    while (true) {
        if (i < a) {
            i = i + 1;
        } else {
            return i;
            print "aoisasoi";
        }
    }
}

sub addi (a,b : int) : int {
    return a + b;
}

k : int = f(10);
print k;

m : int = addi(2,5);
print m;
