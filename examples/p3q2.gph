
x : int;
y : float = 5.8;
z : string = "oi";
k : char = 'a';
b : bool = true;
 
l : [int] = [1,2,3];
t : (int, float) = (1, 2.0);
d : {string, int} = {"a" ? 2, "b" ? 3, "c" ? 4};
g1 : <int> = <[6,6,6], a--b for a,b A>;
g2 : <int, float> = a/2 where a--b+1 for a,b over [0,1],[1,2,3]
g3 : <int> = a--a for a in [0, 1, 2] when a > c

for a in [1,2,3] {

}

l : [int] = [i+j for i,j in [4,4,4],[3,3,3]];



