def friendship(a : int, b : int) : bool {
    if (a != b) and (a == 0 or ceil(a/2) == ceil(b/2)) {
        return true;
	} else {
        return false;
    }
}

f3 : <int> = a--b over [0..7] where friendship(a, b);
f4 : <int> = a--b over [0..9] where friendship(a, b);
