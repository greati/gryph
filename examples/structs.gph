Pessoa {
    n : string;
    p : Point;
}

Point {
    x, y, z : float;
    g : [int];
}

sub abc (i : int&) {
    while (i > 0) {
        print i;
        i = i - 1;
    }
}

sub f (p : Pessoa&; s:string) : Pessoa {
    p{n} = s;
    return p;
}

k : int = 10;
print "k="+k@string;
abc(k);
print "k="+k@string;

i : Pessoa = Pessoa{n = "vitor"};

print (f(i,"kkkkk")){n};
print i{n};
