Pessoa {
    n : string;
    p : Point;
}

Point {
    x, y, z : float;
    g : [int];
}

sub abc (i : int&) {
    while (i > 0) {
        print i;
        i = i - 1;
    }
}

sub f (p : Pessoa&; i : int&; s:string) : Pessoa {
    p{n} = s;
    i = 4;
    return p;
}

k : int = 10;
print "k="+k@string;
abc(k);
print "k="+k@string;

i : Pessoa = Pessoa{n = "vitor"};
m : int = 10;
#f(i, m,"kkkkk");
print (f(i,m,"kkkkk")){n};
print i{n};
print m;
